import { EdDSAPCD } from "@pcd/eddsa-pcd";
import { PCD, StringArgument } from "@pcd/pcd-types";
export declare const EmailPCDTypeName = "email-pcd";
export type EmailPCDArgs = {
    privateKey: StringArgument;
    emailAddress: StringArgument;
    semaphoreId: StringArgument;
    semaphoreV4Id: StringArgument;
    id: StringArgument;
};
export interface EmailPCDClaim {
    emailAddress: string;
    semaphoreId: string;
    semaphoreV4Id?: string;
}
export interface EmailPCDProof {
    eddsaPCD: EdDSAPCD;
}
export declare class EmailPCD implements PCD<EmailPCDClaim, EmailPCDProof> {
    type: string;
    claim: EmailPCDClaim;
    proof: EmailPCDProof;
    id: string;
    constructor(id: string, claim: EmailPCDClaim, proof: EmailPCDProof);
}
export declare const isEmailPCD: (pcd: PCD<unknown, unknown>) => pcd is EmailPCD;
//# sourceMappingURL=EmailPCD.d.ts.map