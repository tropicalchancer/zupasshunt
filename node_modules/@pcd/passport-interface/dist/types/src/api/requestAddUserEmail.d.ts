import { SerializedPCD } from "@pcd/pcd-types";
import { SemaphoreSignaturePCD } from "@pcd/semaphore-signature-pcd";
import { AddUserEmailResponseValue, EmailUpdateError } from "../RequestTypes";
import { APIResult } from "./apiResult";
/**
 * Sends a request to the server to add a new email address to a user's account.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestAddUserEmail(zupassServerUrl: string, newEmail: string, pcd: SerializedPCD<SemaphoreSignaturePCD>, 
/**
 * If absent, requests a confirmation code; if present redeems it and performs the update.
 */
confirmationCode?: string): Promise<AddUserEmailResult>;
export type AddUserEmailResult = APIResult<AddUserEmailResponseValue, EmailUpdateError>;
//# sourceMappingURL=requestAddUserEmail.d.ts.map