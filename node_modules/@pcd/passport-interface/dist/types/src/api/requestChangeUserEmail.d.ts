import { SerializedPCD } from "@pcd/pcd-types";
import { SemaphoreSignaturePCD } from "@pcd/semaphore-signature-pcd";
import { ChangeUserEmailResponseValue, EmailUpdateError } from "../RequestTypes";
import { APIResult } from "./apiResult";
/**
 * Sends a request to the server to change the single email associated
 * with their account to another email address.
 *
 * Never rejects. All information encoded in the resolved response.
 */
export declare function requestChangeUserEmail(zupassServerUrl: string, oldEmail: string, newEmail: string, pcd: SerializedPCD<SemaphoreSignaturePCD>, 
/**
 * If absent, requests a confirmation code; if present redeems it and performs the update.
 */
confirmationCode?: string): Promise<ChangeUserEmailResult>;
export type ChangeUserEmailResult = APIResult<ChangeUserEmailResponseValue, EmailUpdateError>;
//# sourceMappingURL=requestChangeUserEmail.d.ts.map