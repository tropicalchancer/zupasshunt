"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestChangeUserEmail = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * Sends a request to the server to change the single email associated
 * with their account to another email address.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestChangeUserEmail(zupassServerUrl, oldEmail, newEmail, pcd, 
/**
 * If absent, requests a confirmation code; if present redeems it and performs the update.
 */
confirmationCode) {
    return (0, makeRequest_1.httpPost)((0, url_join_1.default)(zupassServerUrl, "/account/change-email"), {
        onValue: async (resText) => ({
            value: JSON.parse(resText),
            success: true
        }),
        onError: async (resText, code) => ({
            error: resText,
            success: false,
            code
        })
    }, {
        oldEmail,
        newEmail,
        pcd,
        confirmationCode
    });
}
exports.requestChangeUserEmail = requestChangeUserEmail;
