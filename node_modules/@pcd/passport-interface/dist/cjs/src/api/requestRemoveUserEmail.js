"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.requestRemoveUserEmail = void 0;
const url_join_1 = __importDefault(require("url-join"));
const makeRequest_1 = require("./makeRequest");
/**
 * Sends a request to the server to remove an email address from being
 * associated with their Zupass account.
 *
 * Never rejects. All information encoded in the resolved response.
 */
async function requestRemoveUserEmail(zupassServerUrl, emailToRemove, pcd) {
    return (0, makeRequest_1.httpPost)((0, url_join_1.default)(zupassServerUrl, "/account/delete-email"), {
        onValue: async (resText) => ({
            value: JSON.parse(resText),
            success: true
        }),
        onError: async (resText, code) => ({
            error: resText,
            success: false,
            code
        })
    }, {
        emailToRemove,
        pcd
    });
}
exports.requestRemoveUserEmail = requestRemoveUserEmail;
